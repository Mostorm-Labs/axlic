---
authority_id: AXL-V1-A0-P34
stage: P34R
scope: axlicense/A0
kind: gate-rereview
version: "1"
status: historical
source_type: notion
source_url: https://app.notion.com/p/3db4c57a590c8144aaa4feb2f963539f
migration_class: location-only
semantic_change: none
---

# 34R — P34 AXL-V1-A0 Gate Rereview — PASS_WITH_FINDINGS

> ✅
>
> **Gate verdict: PASS_WITH_FINDINGS / CLOSED — 2026-09-14.** `A0-P34-B1` 已在 repaired result `13d16ebb34913dafbddc83222cf29187e92c8053` 上关闭。Frozen requirements、blocking evidence 与 repository reality 均满足 AXL-V1-A0 P31 closure contract；本轮未发现新的 blocking high-impact failure mode。

## 1. Exact review identity

- Repository: `github/Mostorm-Labs/axlic`
- Governing package: `6190288cf7d83bcbe588a4e19fda8d6354335e6f:.aegis/packages/AXL-V1-A0-P31.md`
- Task anchor: `5a2cb0fc105193523e518cd9c109ccae35bb2188`
- Historical blocked result: `09920aa719e93f076a04cfc62717104c547956d3`
- Repaired result revision: `13d16ebb34913dafbddc83222cf29187e92c8053`
- Materialized ref: `refs/heads/codex/axl-v1-a0@13d16ebb34913dafbddc83222cf29187e92c8053`
- Hosted run: `34824491876`, attempt 1 — SUCCESS
- Job: `103913345075` — SUCCESS
- Evidence artifact: `10340300160`, `AXL-V1-A0-evidence`
- Artifact digest: `sha256:e1c696436a1377a5ce1d3671abe5aab6c4a5470400516fc7d7d5a41cee87cfe7`

## 2. Frozen Requirement Audit

### PASS

- Package materialization commit `6190288…` is a real ancestor of `13d16ebb…`; result is repository-resolvable on the frozen execution branch.
- A0 remains client-only. No `server/**`, PostgreSQL, HTTP server, DeviceIdentity establishment, activation, refresh, rehost, factory/admin implementation or public DLL ABI was introduced.
- `A0-P34-B1 / FR-A0-DEP-QCBOR` is repaired: QCBOR exact commit `930708bb86481e88879eb1d87fd4d664f1d69503` is fetched and linked as `qcbor::qcbor`; production QCBOR use is isolated behind `client/src/wire/qcbor_codec.*`.
- Project-owned generic CBOR reader/writer machinery has been removed from `credential.cpp`; AxLicense-specific bounds/canonicality/semantic policy remains above the library boundary.
- Windows CNG/BCrypt remains the production P-256/SHA-256 verifier; fixed 64-byte low-S and exact received-byte signature verification semantics are preserved.
- RuntimeLicense, A0 fixture seam, `status` / `entitlement` CommandResult 1.0 semantics and reference vectors remain unchanged by the repair.
- Third-party notice and toolchain identity now record the frozen QCBOR commit.

### FAIL

- None.

## 3. Frozen Evidence Audit

### PASS

- T-A0-01: Windows x64 Release configure/build PASS; `qcbor.lib`, `axlic_core`, `axlic.exe` and tests build successfully.
- T-A0-02 / EV-01: production corpus PASS for all frozen positives and negatives, including tamper, wrong key/algorithm/domain, unknown schema, non-minimal/indefinite/tag/duplicate/wrong-order/null, signature length/range/high-S, oversized/nesting/item-count cases.
- T-A0-03: independent Python reference regeneration PASS; corpus hash matches production report; independent `cryptography==50.0.1` verifier validates frozen fixed-width low-S positive signatures.
- T-A0-04 / EV-06: process-level CLI contract PASS for valid/absent/invalid/unsupported states and entitlement presence/absence.
- T-A0-05: offline/no-server boundary PASS; no forbidden server/database imports were reported.
- T-A0-06: W1 baseline materialized with 20 warmups + 200 measurements; p50 `4.918 ms`, p95 `5.574 ms`, p99 `5.875 ms`. Numeric threshold remains corroborative at A0.
- Reviewer artifact is downloadable, unexpired, contains six expected machine-readable evidence files, and every file binds `implementation_revision` to `13d16ebb…`.

### FAIL

- None.

## 4. Repository Reality Audit

- `6190288… → 13d16ebb…`: 4 commits ahead, 0 behind; all substantive implementation remains within A0-authorized surfaces.
- `09920aa… → 13d16ebb…`: repair touches only CMake/dependency metadata, wire codec boundary, third-party notice and toolchain evidence identity.
- `client/CMakeLists.txt` freezes QCBOR commit, disables float/tags/indefinite forms/non-integer labels/exp-mantissa support where applicable, and links `qcbor::qcbor` privately into `axlic_core`.
- `qcbor_codec.cpp` enforces bounded artifact/text/item/nesting/entitlement limits and uses QCBOR for encode/decode; `credential.cpp` independently re-encodes for canonical equality before acceptance and verifies the original payload bytes.
- No production reference-Python dependency or test fixture private key dependency is introduced.

## 5. New findings / closure stability

### NON_BLOCKING_FINDING — independent-reference report granularity

`independent-reference.json` explicitly enumerates only positive vectors for the standards-verifier step. The frozen negative corpus is nevertheless independently generated by the Python reference implementation and is consumed by production C++ fail-closed tests; EV-01 lists every negative as PASS. Requiring a second standards-verifier rejection record for the same malformed/tampered corpus would add confidence but does not close a distinct residual high-impact failure mode. Under the P20 evidence principle and Anti-Proof-Recursion rule, this is evidence-report hygiene / successor hardening, not a Gate blocker.

### Other preserved non-blocking findings

- Exact Visual Studio patch-label drift remains non-blocking because the frozen MSVC 19.44/toolset 14.44 and Windows SDK 10.0.26100.0 identities match the required family/API boundary.
- `actions/upload-artifact` Node runtime deprecation warning is tooling hardening only; artifact upload succeeded and evidence identity is intact.
- An ancestry-only content-identical noop commit `24350f…` exists before the repair commit; it changes no tree bytes and does not alter result/evidence applicability.
- README/git metadata additions remain non-substantive A0 repository hygiene.

## 6. Gate result

```yaml
gate_review:
  frozen_requirements:
    passed:
      - A0 authorized/forbidden scope
      - FR-A0-DEP-QCBOR production dependency and wrapper boundary
      - credential canonical/crypto behavior
      - RuntimeLicense and CommandResult 1.0 behavior
      - hosted exact-result materialization boundary
    failed: []
  frozen_evidence:
    passed:
      - T-A0-01
      - T-A0-02 / EV-01
      - T-A0-03 independent reference
      - T-A0-04 / EV-06
      - T-A0-05
      - T-A0-06 baseline materialization
      - exact run/job/artifact binding
    failed: []
  new_findings:
    blocking_high_impact: []
    non_blocking:
      - independent-reference negative-result reporting can be strengthened without unique new detection value
      - exact VS patch-label drift
      - upload-artifact Node runtime deprecation
      - ancestry-only noop commit
      - README/git metadata hygiene
  verdict: PASS_WITH_FINDINGS
```

## 7. Closure and route

**AXL-V1-A0 P34 Gate is CLOSED.** D-093 remains historical evidence of the first blocked review; D-094 remains the P36 repair/reverification record. This rereview supersedes the active Gate status for A0 but does not erase prior audit history.

Return to **central `aegis` successor routing**. The next owner must decide whether A0 requires Repository Integration Closure before A1 ingress; P34 does not directly authorize A1 or merge/integration work.
